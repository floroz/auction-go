global:
  postgresql:
    auth:
      postgresPassword: password
      username: user
      password: password
      database: stats_db

primary:
  persistence:
    enabled: false # Ephemeral for local dev

